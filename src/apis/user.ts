/**
 * user api.
 */
import { UserId, UserSchema } from '../schemas';
import { UserFetch } from '../fetchs';

class userApi {
    login(username: string, password: string) {

    }
}

export const UserAPI = new userApi;